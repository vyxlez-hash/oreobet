import { createContext,useContext,useEffect,useMemo,useState } from "react";import type {Session,User} from "@supabase/supabase-js";import {supabase} from "../lib/supabase";
export type Profile={id:string;username:string;balance:number;level:number;verified:boolean;username_chosen:boolean;is_admin:boolean;avatar_url:string|null};type AuthState={session:Session|null;user:User|null;profile:Profile|null;loading:boolean;error:string|null;refreshProfile:()=>Promise<Profile|null>;signOut:()=>Promise<void>};const AuthContext=createContext<AuthState|null>(null);
function avatarFor(id:string){return `https://api.dicebear.com/9.x/adventurer/svg?seed=${encodeURIComponent(id.replace(/-/g,""))}`}
const cols="id,username,balance,level,verified,username_chosen,is_admin,avatar_url";
async function ensureProfile(user:User):Promise<Profile>{if(!supabase)throw new Error("Supabase environment variables are missing.");let q=await supabase.from("profiles").select(cols).eq("id",user.id).maybeSingle();if(q.error)throw q.error;if(q.data){if(!q.data.avatar_url){const avatar_url=avatarFor(user.id);const u=await supabase.from("profiles").update({avatar_url}).eq("id",user.id).select(cols).single();if(!u.error&&u.data)return u.data as Profile;}return q.data as Profile;}const base=String(user.user_metadata?.username||user.email?.split("@")[0]||"user").replace(/\s+/g,"_").replace(/[^a-zA-Z0-9_]/g,"").slice(0,15)||"user";const username=`${base}_${user.id.replace(/-/g,"").slice(0,4)}`.slice(0,20);const ins=await supabase.from("profiles").insert({id:user.id,username,balance:0,level:1,verified:false,username_chosen:false,is_admin:false,avatar_url:avatarFor(user.id)}).select(cols).single();if(ins.error){const retry=await supabase.from("profiles").select(cols).eq("id",user.id).maybeSingle();if(retry.data)return retry.data as Profile;throw ins.error}return ins.data as Profile}
export function AuthProvider({children}:{children:React.ReactNode}){const[session,setSession]=useState<Session|null>(null);const[profile,setProfile]=useState<Profile|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState<string|null>(null);const refreshProfile=async()=>{if(!session?.user)return null;try{const p=await ensureProfile(session.user);setProfile(p);setError(null);return p}catch(e){setError(e instanceof Error?e.message:"Unable to load profile");return null}};useEffect(()=>{if(!supabase){setError("Supabase environment variables are missing.");setLoading(false);return;}let alive=true;(async()=>{const {data}=await supabase.auth.getSession();if(!alive)return;setSession(data.session);if(data.session?.user){const p=await ensureProfile(data.session.user).catch(e=>{setError(e instanceof Error?e.message:"Unable to load profile");return null});if(alive)setProfile(p)}setLoading(false)})();const {data:listener}=supabase.auth.onAuthStateChange((_event,next)=>{setSession(next);if(!next){setProfile(null);return;}setTimeout(()=>{ensureProfile(next.user).then(setProfile).catch(e=>setError(e instanceof Error?e.message:"Unable to load profile"))},0)});return()=>{alive=false;listener.subscription.unsubscribe()};},[]);const value=useMemo<AuthState>(()=>({session,user:session?.user??null,profile,loading,error,refreshProfile,signOut:async()=>{if(supabase)await supabase.auth.signOut()} }),[session,profile,loading,error]);return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>}
export function useAuth(){const value=useContext(AuthContext);if(!value)throw new Error("useAuth must be used inside AuthProvider");return value}
